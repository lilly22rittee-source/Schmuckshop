body { font-family: Arial, sans-serif; margin:0; padding:0; }
header { background: #f5f5f5; padding: 20px; text-align: center; }
header nav a { margin: 0 10px; text-decoration: none; color: #333; }
h1 { margin: 0; }
.produktliste { display: flex; flex-wrap: wrap; justify-content: center; gap: 20px; padding: 20px; }
.produkt { border: 1px solid #ddd; padding: 10px; width: 200px; text-align: center; }
.produkt img { width: 100%; height: auto; }
button { background: #ff69b4; color: white; border: none; padding: 10px; cursor: pointer; }
button:hover { background: #ff1493; }
